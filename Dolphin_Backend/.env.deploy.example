# Disabled: Deployment template (local-only project)

APP_NAME=Dolphin
APP_ENV=local
APP_KEY=   # <-- Set in Render (e.g. base64:...)
APP_DEBUG=false
APP_URL=http://127.0.0.1:8000

# Frontend URL for CORS and links (local-only)
FRONTEND_URL=http://127.0.0.1:8080

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_LEVEL=debug

# Database - Local MySQL
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=dolphin_db
DB_USERNAME=dolphin123
DB_PASSWORD=dolphin123

SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_STORE=database
QUEUE_CONNECTION=database
FILESYSTEM_DISK=local

# Redis (optional)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail - log locally
MAIL_MAILER=log
MAIL_FROM_ADDRESS=no-reply@localhost
MAIL_FROM_NAME=Dolphin

# Stripe (optional for local)
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# Other third-party keys (unused locally)
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_PLAN=

# Optional: logging, caching
LOG_STACK=single

# NOTE: This template is kept for reference but configured for local-only use.
